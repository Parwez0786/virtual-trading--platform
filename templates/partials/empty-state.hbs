<div class="ui-empty" role="status">
  <h2 class="ui-empty__title">{{title}}</h2>
  <p class="ui-empty__text">{{text}}</p>
  {{#if href}}
  <div class="ui-empty__actions">
    <a href="{{href}}">{{#if cta}}{{cta}}{{else}}Go back{{/if}}</a>
  </div>
  {{/if}}
</div>
